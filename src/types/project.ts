export interface Project {
  title: string;
  subheader: string;
  description: string[];
  image: string;
  tags: string[];
  github?: string;
  documentation?: string;
  preview?: string;
  videoUrl?: string;
}
